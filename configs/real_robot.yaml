robot:
  control_rate_hz: 15
  max_linear_speed: 0.5
  max_angular_speed: 1.0
  safety_min_distance: 0.4
  wheel_base: 0.3

policy:
  artifact_path: "exported_policies/ppo_rover.ts"

lidar:
  num_rays: 64
  fov_deg: 270.0
  max_range: 8.0

logging:
  telemetry_path: "telemetry_logs/real_robot.jsonl"

seed: 123

